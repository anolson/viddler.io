<!DOCTYPE html>
<html>
<head>
  <title>viddler.io</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
  <body>
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="brand" href="#">viddler.io</a>
          <% if @current_user %>
            <ul class="nav">
              <li><%= link_to "Videos", videos_path %></li>
              <li><%= link_to "Upload", new_video_path %></li>
            </ul>
            <div class="pull-right">
              <p class="navbar-text">Logged in a <strong><%= @current_user[:username] %></strong>. <%= link_to "Logout", logout_path %></p>
            </div>
          <% end %>
        </div>
      </div>
    </div>
    <div class="container">
      <%= yield %>  
    </div>
  </body>
</html>
